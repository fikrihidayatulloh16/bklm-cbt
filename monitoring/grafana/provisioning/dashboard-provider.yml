apiVersion: 1
providers:
  - name: 'BKLM Provisioned Dashboards'
    orgId: 1
    folder: ''
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      # Jalur ini adalah folder di DALAM container Grafana nanti
      path: /var/lib/grafana/dashboards